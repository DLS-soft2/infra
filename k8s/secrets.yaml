apiVersion: v1
kind: Secret
metadata:
  name: dls-secrets
  namespace: dls
type: Opaque
stringData:
  # Placeholders only -- deploy.sh patches in the real values from infra/docker/.env
  # (gitignored). NEVER commit real credentials here.
  google-client-id: google-oauth-not-configured
  google-client-secret: google-oauth-not-configured
data:
  postgres-password: cG9zdGdyZXM=
  keycloak-admin-password: YWRtaW4=
